<UserControl x:Class="TraderVI.WPF.Views.DelphiSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:TraderVI.WPF.Views"
             Background="#11151B" Foreground="#F4F7FB" FontFamily="Segoe UI">
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="#1A2029"/>
            <Setter Property="BorderBrush" Value="#2E3949"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="18"/>
            <Setter Property="Margin" Value="0,0,0,14"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="IsReadOnly" Value="{Binding DataContext.RulesReadOnly,RelativeSource={RelativeSource AncestorType=UserControl}}"/>
            <Setter Property="Background" Value="#11151B"/>
            <Setter Property="Foreground" Value="#F4F7FB"/>
            <Setter Property="BorderBrush" Value="#536078"/>
            <Setter Property="Padding" Value="9,7"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="CaretBrush" Value="White"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="16,9"/>
            <Setter Property="Foreground" Value="#11151B"/>
            <Setter Property="Background" Value="#D1B7FF"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </UserControl.Resources>
    <Grid Margin="0,14,0,0">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <Border Style="{StaticResource Card}" Background="#211C2E">
            <StackPanel>
                <TextBlock Text="DAILY ENGINE SETTINGS" Foreground="#B58CFF" FontWeight="SemiBold" FontSize="12"/>
                <TextBlock Text="Daily Delphi combines a preserved four-model set with decision gates to produce Continuation and Breakout recommendations. These are shared inputs for downstream paper systems; pause and clear dependent accounts before changing this version." TextWrapping="Wrap" Margin="0,8,0,8"/>
                <views:SystemControlBar SystemKey="Daily"/>
                <TextBlock Text="{Binding AccessDescription}" Foreground="#EBC98C"/>
                <TextBlock Text="{Binding ActiveSummary}" FontSize="21" FontWeight="SemiBold" Margin="0,6,0,8"/>
                <TextBlock Text="{Binding Status}" Foreground="#C2CBD8"/>
            </StackPanel>
        </Border>
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel IsEnabled="{Binding CanEdit}" Margin="0,0,12,0">
                <Border Style="{StaticResource Card}">
                    <StackPanel>
                        <DockPanel Margin="0,0,0,10">
                            <Button Content="Reload" DockPanel.Dock="Right" Click="Reload_Click"/>
                            <TextBlock Text="Model set &amp; strategy" FontSize="19" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        </DockPanel>
                        <TextBlock Text="Delphi combines four trained models. Select a preserved set below to load the strategy and thresholds saved with it." Foreground="#9BA8BA" Margin="0,0,0,12"/>
                        <ComboBox ItemsSource="{Binding Strategies}" SelectedItem="{Binding SelectedStrategy}" DisplayMemberPath="DisplayName"
                                  Foreground="#11151B" Background="#EAF0F8" FontSize="15" Padding="10,8"
                                  AutomationProperties.Name="Saved model set and strategy"/>
                        <TextBlock Text="{Binding PolicySummary}" Foreground="#C2CBD8" Margin="0,12,0,0"/>
                    </StackPanel>
                </Border>
                <ItemsControl ItemsSource="{Binding Models}">
                    <ItemsControl.ItemsPanel><ItemsPanelTemplate><UniformGrid Columns="2"/></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource Card}" Padding="14" Margin="0,0,10,14">
                                <StackPanel>
                                    <TextBlock Text="{Binding Task}" Foreground="#B58CFF" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="SemiBold" Margin="0,4,0,5"/>
                                    <TextBlock Text="{Binding Features}" Foreground="#C2CBD8"/>
                                    <TextBlock Text="{Binding Signals}" Foreground="#C2CBD8"/>
                                    <TextBlock Text="{Binding Training}" Foreground="#9BA8BA" Margin="0,4,0,0"/>
                                    <TextBlock Text="{Binding Id, StringFormat='Model ID: {0}'}" Foreground="#9BA8BA" FontSize="11"/>
                                    <TextBlock Text="{Binding Hash, StringFormat='SHA-256: {0}'}" Foreground="#9BA8BA" FontSize="11"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <Border Style="{StaticResource Card}">
                    <StackPanel>
                        <TextBlock Text="Decision thresholds" FontSize="19" FontWeight="SemiBold"/>
                        <TextBlock Text="Use decimal fractions: 0.35 means 35%. Save Version creates a new strategy version, preserving the original settings and evidence." Foreground="#9BA8BA" Margin="0,7,0,14"/>
                        <ItemsControl ItemsSource="{Binding Thresholds}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,0,0,14">
                                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="135"/></Grid.ColumnDefinitions>
                                        <StackPanel Margin="0,0,18,0">
                                            <TextBlock Text="{Binding Label}" FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding Help}" Foreground="#9BA8BA" FontSize="12" Margin="0,3,0,0" TextWrapping="Wrap"/>
                                        </StackPanel>
                                        <TextBox Grid.Column="1" Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center"
                                                 IsReadOnly="{Binding DataContext.RulesReadOnly,RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                 AutomationProperties.Name="{Binding Label}"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <TextBlock Text="{Binding PreservedSettings}" Foreground="#9BA8BA" FontSize="12"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource Card}">
                    <StackPanel>
                        <TextBlock Text="Save version &amp; assign" FontSize="19" FontWeight="SemiBold"/>
                        <TextBlock Text="New version name (for Save Version)" Margin="0,12,0,6"/>
                        <TextBox Text="{Binding NewVersionName, UpdateSourceTrigger=PropertyChanged}" MaxLength="32" AutomationProperties.Name="New strategy name"/>
                        <TextBlock Text="Reason for this selection or threshold change" Margin="0,12,0,6"/>
                        <TextBox Text="{Binding ReviewReason, UpdateSourceTrigger=PropertyChanged}" MaxLength="350" AutomationProperties.Name="Review reason"/>
                        <TextBlock Text="Save Version preserves your edits without changing the engine. Assign uses a saved version immediately and starts an official daily reevaluation. Model candidates become selectable after a reviewed model set is registered." Foreground="#9BA8BA" Margin="0,14,0,14"/>
                        <WrapPanel><Button Content="Review and save version" Click="Save_Click" IsEnabled="{Binding CanWrite}" Margin="0,0,12,0"/><Button Content="Assign saved version…" Click="Review_Click" IsEnabled="{Binding CanWrite}"/><Button Content="Discard draft" Click="Discard_Click" Margin="12,0,0,0"/></WrapPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
